// types/user.ts (optional, but a good practice to separate types)
export interface User {
    name: string;
    email: string;
    image?: string; // Optional property for user image (if needed)
  }
  